# config/packages/framework.yaml
when@prod:
    framework:
        http_cache: true

when@dev:
    _errors:
        resource: '@FrameworkBundle/Resources/config/routing/errors.xml'
        prefix: /_error
